# Test-time only. CI installs this on top of requirements.txt.
-r requirements.txt
pytest
# The DynamoDB state backend (state_store.py) is exercised against moto's
# in-process DynamoDB so the suite needs no AWS account and no docker.
boto3>=1.34,<2
moto[dynamodb]>=5,<6
