# Upper bounds pin the major (and, for 0.x, the minor) so a server rebuild
# can't silently adopt a breaking release between deploys. Raise a ceiling
# deliberately after testing the new version.
fastapi>=0.115,<0.200
uvicorn[standard]>=0.34,<0.40
openai>=1.82,<3
duckdb>=1.2,<2
pandas>=2.2,<3
pyyaml>=6,<7
requests>=2.31,<3
