# Upper bounds so a server rebuild can't silently adopt a breaking release.
# For 0.x packages (fastapi, uvicorn) breaking changes ship in MINOR releases,
# so those are pinned to a single minor; the rest are capped at the next major.
# Raise a ceiling deliberately after testing the new version.
fastapi>=0.115,<0.116
uvicorn[standard]>=0.34,<0.35
openai>=1.82,<3
duckdb>=1.5.5,<2
pandas>=2.2,<3
pyyaml>=6,<7
requests>=2.31,<3
